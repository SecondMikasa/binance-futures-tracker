# Rename this file to `.env` and provide your Neon/Postgres connection string
# You can find the connection string on the Neon dashboard under the "Connection Info" section.

DATABASE_URL=postgres://<username>:<password>@<host>:<port>/<database>

# Optional port for the backend server (defaults to 4000)
PORT=4000
